var functionToTest = require('../countAllPaarl');
let assert = require("assert");

describe ("count all parl", function(){

it ("count all from paarl", function(){
    assert.equal(countAllPaarl(reg));
})

})