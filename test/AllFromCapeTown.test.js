var functionToTest = require('../AllFromCapeTown.js');
let assert = require("assert");

describe ("all from Cape Town", function(){

    it ("should find all from Cape Town", function(){

        assert.equal(countAllFromTown(reg, loc));
    })
}) 